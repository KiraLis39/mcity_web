# Наш первый проект на Node.js (Next.js) и Java

## Точка запуска приложения - файл `index.js` в корне.

### Запуск веб-морды (сервера):
Командой в терминале ```npm run start-dev``` 

Остановка через `Ctrl+C`
